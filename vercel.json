{
  "devCommand": "vite --port $PORT",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/__elysia" },
    { "source": "/:path((?!api/).*)", "destination": "/" }
  ],
  "crons": [
    { "path": "/api/cron/kill-sessions", "schedule": "0 0 * * *" }
  ]
}
